[package]
name = "leaky"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.0", optional = true, features = ["derive"] }


[features]
serde = ["dep:serde"]
default = ["serde"]